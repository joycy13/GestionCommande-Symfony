{{ form_start(form) }}
    {{ form_widget(form) }}
    <br/>
    <button class="btn btn-outline-primary" style="color: #000000">
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
