{% extends 'base.html.twig' %}

{% block title %}Statuts{% endblock %}

{% block body %}
    </br>
    <b><font face="Optima, sans-serif"><h1>Statuts</h1></font></b>
    </br>
    <div class="container">
    <font face="Optima, sans-serif"><table class="table"></font>
        <thead>
            <tr style="background-color: #B10E33; color: #FFFFFF">
                <th>Identifiant</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for statut in statuts %}
            <tr style="background-color: #FFE4EB;">
                <td>{{ statut.id }}</td>
                <td>
                    <button type="button" class="btn btn-outline-warning">
                        <a href="{{ path('statut_show', {'id': statut.id}) }}" style="color: #000000;">Informations</a>
                    </button>
                    <button type="button" class="btn btn-outline-success">
                        <a href="{{ path('statut_edit', {'id': statut.id}) }}" style="color: #000000;">Modifier</a>
                    </button>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div style="text-align: center;">
        <button type="button" class="btn btn-outline-primary">
            <a href="{{ path('statut_new') }}" style="color: #000000;">Ajouter un statut</a>
        </button>
    </div> 
    <br/>   
    </div>
{% endblock %}
